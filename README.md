# nitter-load-balancer

A self-hosted load balancer for nitter rss powered by https://status.d420.de/

## Quickstart

1. clone this repo
2. run `./gradlew bootRun`
3. browse `localhost:8080/<twitter ID>/rss` to get the RSS feed
